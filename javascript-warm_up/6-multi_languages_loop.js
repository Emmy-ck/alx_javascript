#!/usr/bin/node

// A script that pints 3 lines by using an array of string and a loop

let printWords = ['C is fun', 'Python is cool', 'JavaScript is amazing']

for (let i = 0; i < printWords.length; i++) {
    console.log(printWords[i])
}